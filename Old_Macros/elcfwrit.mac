!
/NOPR


*ASK,NAME,COMPONENT NAME ( 8 Characters max.),'comp'
*IF,ARG1,LT,1,THEN
*ASK,STRT,Starting ELCF Loc. number,1
*ELSE
STRT=ARG1
*ENDIF
*IF,ARG2,LT,1,THEN
*ASK,EEND,Ending ELCF Loc. number,99
*ELSE
EEND=ARG2
*ENDIF


CMSEL,,LOC%LOOP%
*GET,NELES,ELEM,,COUNT
*DIM,ELES,,NELES
EL=ELNEXT(0)
*DO,L,1,NELES
ELES(L)=EL
EL= ELNEXT(EL)
*ENDDO

*VWRITE,ELES(1)
(6*(A,","))
