PARSAV
*GET,NELES,ELEM,,COUNT
ELE=ELNEXT(0)
cfopen,junk
*ASK,RARG,WHICH REAL CONSTANT PARM,3
*DO,LOOP,1,NELES
*GET,EREAL,ELEM,ELE,ATTR,REAL
*GET,VALU,RCON,EREAL,CONST,RARG
LOCX=(NX(NELEM(ELE,1))+NX(NELEM(ELE,2)))/2
LOCY=(NY(NELEM(ELE,1))+NY(NELEM(ELE,2)))/2
LOCZ=(NZ(NELEM(ELE,1))+NZ(NELEM(ELE,2)))/2
*CFWRIT,/AN3D,TEXT,LOCX,LOCY,LOCZ,r=%VALU%
ELE=ELNEXT(0)
*ENDO
PARRES
