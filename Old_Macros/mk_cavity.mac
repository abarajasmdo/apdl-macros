!  eases the generation of pressure cavity
/NOPR
/prep7
/PMACRO
PARSAV
*GET,NUM_LS,LINE,,COUNT
*DO,LOOP,1,10,
CM,START_,LINE
*ASK,CAVNUM,CAVITY NUMBER TO CREATE (ZERO TO EXIT),0
*IF,CAVNUM,EQ,0,*EXIT
LSEL,R,P
*GET,N_LS,LINE,,COUNT
*IF,N_LS,EQ,NUM_LS,*CYCLE
*GET,CHECK,COMP,CAV%CAVNUM%,TYPE
*IF,CHECK,GT,0,THEN
CMSEL,A,CAV%CAVNUM%
*ENDIF
CM,CAV%CAVNUM%,LINE
LATT,CAVNUM
!KPLS
!CM,KPS_,KP
CMSEL,,START_
cmsel,u,CAV%CAVNUM%
LPLO
*ENDDO
CMDELE,START_
PARRES

/GOPR
