!
!  box.mac  Selects box of nodes in active coordinate system
!
!    Select four nodes that define the edges of the box.
!
*get,left,ny,min
*get,rght,ny,max
*get,top,nx,max
*get,bott,nx,min
nsel,y,left,rght
nrsel,x,bott,top
