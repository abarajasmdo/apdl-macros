!
! macro DBEND
!
! input  DBEND,node1,node2,radius
!
! macro builds pipe elements jointed at node1 and makes elem n1 to n2 
!
rsize,12     ! why? when I don't use 12
e,arg1,arg2
nsel,node,arg1
enode
*get,e1,elmx
*get,e2,elmn
nall
eall
bend,e1,e2,arg3

